import React, { useState, useEffect } from 'react';
import '../styles/groups.css';

const Groups = ({ poll, showVotersMode }) => {
    return (
        <h1>Hello World!</h1>
    );
};

export default Groups;
